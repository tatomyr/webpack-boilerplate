import React from 'react';

import './style.scss';

export default props => (
  <div>
    <h1>Hello Server + SET DEPLOYMENT + ESLint + Snyk + automated start</h1>
    <div>{props.text}</div>
  </div>
);
