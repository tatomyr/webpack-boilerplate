import React from 'react';

import './style.scss';

export default props => (
  <div>
    <h1>Hello Server + SET DEPLOYMENT + ESLint + Snyk + automated start + sourcemap | Webpack Config Refactoring</h1>
    <div>{props.text}</div>
    <img src="/assets/PNG.png" alt="x" />
  </div>
);
